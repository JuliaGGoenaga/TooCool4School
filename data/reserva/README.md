# Fichas de reserva

Fichas guardadas para el futuro que **no** carga la web (build.py solo lee
`data/estrategias/*.json`). Para reincorporar una, muévela a `data/estrategias/`,
ajusta su `codigo`/`imagen` y ejecuta `python3 build.py`.
